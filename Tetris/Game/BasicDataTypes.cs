namespace Tetris.Game.BasicDataTypes
{
    public enum Direction { Left, Up, Right, Down }

    public enum State { NotStarted, Running, Paused, Over }

    public enum Difficulty { Easy, Normal, Hard }
}
